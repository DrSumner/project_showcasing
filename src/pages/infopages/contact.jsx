import React from "react";
import { useEffect, useState } from "react";


const Contact = (props) => {
    const {info} = props
  
    return (
      <div>
      </div>
    );
};

export default Contact;